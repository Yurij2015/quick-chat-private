[
    {
        "name": "Quick Chat",
        "ip": "138.201.196.61",
        "port": 2228,
        "username": "yurii",
        "path": "/home/yurii/quick-chat",
        "beforeHooks": "echo 'Starting deployment for Quick Chat...'",
        "afterHooks": "cd $RELEASE_PATH && docker compose -p chat-app --env-file /home/yurii/quick-chat/.env -f compose.yaml down --remove-orphans && docker rm -f chat-app chat-mysql chat-redis chat-reverb chat-worker 2>/dev/null || true && docker compose -p chat-app --env-file /home/yurii/quick-chat/.env -f compose.yaml build --no-cache --pull laravel.test reverb worker && docker compose -p chat-app --env-file /home/yurii/quick-chat/.env -f compose.yaml up -d --remove-orphans && sleep 10 && docker exec chat-app php artisan migrate --force",
        "backupImages": false
    }
]
